pipeline{
  agent any;
   stages{
    stage('Build'){
      steps{
       echo "this is build state"
      }
   }
     stage('deploy'){
      steps{
       echo "this is deploy state"
      }
   }
     stage('test'){
      steps{
       echo "this is test state"
      }
   }
}
}
